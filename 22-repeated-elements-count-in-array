#include <iostream>
using namespace std;

int  ReadPositivNumber(string massge) {


    int  number = 0;

    do {
        cout << massge << endl;
        cin >> number;


    } while (number <= 0);


    return number;

}

void ReadArray(int Arr[100],int &Length) {
    cout << "enter size of array :";
    cin >> Length;
    cout << endl;

    for (int i = 0; i < Length; i++) {
    
        cout << "Enter Element [" << i+1 << "]:";
        cin >> Arr[i];
        cout << endl;
    }

  

}



void PrintArray(int Arr[100], int length) {

    for (int i = 0; i < length;i++) {
    
        cout << Arr[i]<<"  ";
    
    }




}

int TimesRepeated(int Arr[100], int Lengh, int NumberToCheck) {
    int counter = 0;
    for (int i = 0; i < Lengh; i++) {
    
    if (Arr[i]==NumberToCheck) {
        counter++;
    
    
    }
    
    
    }

    return counter;


}

int main()
{
    int Arr[100], Length, NumberToCheck;

    ReadArray(Arr,Length);
    NumberToCheck = ReadPositivNumber("Enter the number you want to check: ");

    cout << "\n Original Array :";
    PrintArray(Arr, Length);



    cout << "\n NUmber " << NumberToCheck;
    cout << "is Repeated";
    cout << TimesRepeated(Arr,Length,NumberToCheck);



	return 0;

}
 
